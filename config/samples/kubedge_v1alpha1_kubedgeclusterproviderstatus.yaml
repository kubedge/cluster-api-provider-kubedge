apiVersion: kubedge.kubedge.cloud/v1alpha1
kind: KubedgeClusterProviderStatus
metadata:
  labels:
    controller-tools.k8s.io: "1.0"
  name: kubedgeclusterproviderstatus-sample
spec:
  # Add fields here
  foo: bar
